#include <gtest/gtest.h>
#include <tvm/ffi/extra/module.h>

namespace {

using namespace tvm::ffi;

TEST(Module, ImportLookupCache) {
  // Module m = Module::LoadFromFile("test_module.so");
}

}  // namespace